import React from "react";

export const metadata = {
  title: "Compare Products",
  description: "Compare Products Page",
};
const layout = ({ children }) => {
  return <div>{children}</div>;
};

export default layout;
