import React from "react";

export const metadata = {
  title: "Complain and Feedback",
  description: "Complain and Feedback Page",
};

const layout = ({ children }) => {
  return <div> {children} </div>;
};

export default layout;
